<p>I make electronic music, too! The data below is presented from a simple JSON file and manipulated into the what is shown.</p>
<div id="music-data-container">
  <div id="music-data">
    <div class="artist">{{ results.artist }}</div>
    <div class="urls">
      <div class="url" v-for="item in results.urls">
        <a href="{{ item.url }}">{{ item.type }}</a>
      </div>
    </div>
    <div class="albums">
      <div class="album" v-for="album in results.albums">
        {{ album.title }} has {{ album.tracks.length }} songs
      </div>
    </div>
  </div>
</div>
